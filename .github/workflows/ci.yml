name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-and-test:
    name: Build & Test (.NET)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore api.sln

      - name: Build
        run: dotnet build api.sln --no-restore --configuration Release

      - name: Test
        run: >
          dotnet test api.Tests/api.Tests.csproj
          --no-build
          --configuration Release
          --logger trx
          --results-directory TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: test-results
          path: TestResults

  build-frontend:
    name: Build & Test (NodeJS)
    runs-on: ubuntu-latest
    # Run inside the prebuilt CI image (Playwright browsers + system libs +
    # Node 24 + baked npm deps), published by build-ci-image.yml. This removes
    # the per-run `npm ci` + `playwright install --with-deps` cost. --ipc=host
    # is Playwright's recommended flag to stop Chromium running out of memory.
    container:
      image: ghcr.io/greven145/yet-another-factory-planner/frontend-ci@sha256:56b9e36b41b193f3f911b85d8af6a67867b506b974b3ad66a3e1864ae7ee85f4 # :latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
      options: --ipc=host

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      # The baked dependencies live at /opt/ci/node_modules in the image; the
      # checkout above is a fresh tree, so copy them into the workspace. The
      # image is rebuilt whenever client/package-lock.json changes, so this
      # stays in sync with the committed lockfile.
      - name: Restore baked node_modules
        working-directory: client
        run: cp -a /opt/ci/node_modules ./node_modules

      - name: Lint (ESLint + jsx-a11y)
        working-directory: client
        run: npm run lint

      - name: Unit tests (Vitest + jest-axe)
        working-directory: client
        run: npm run test

      - name: Build
        working-directory: client
        run: npm run build

      - name: Lighthouse accessibility score
        working-directory: client
        run: npm run a11y:lhci

      # Blocking gate: the scanned surfaces must report zero WCAG 2.0 A/AA
      # violations (axe-core via Playwright). The graph canvas is excluded as a
      # separate tracked follow-up; see client/tests/a11y.spec.ts.
      # GitHub sets HOME=/github/home for container jobs and that dir isn't
      # owned by root, so Firefox/WebKit refuse to launch ("$HOME isn't owned
      # by the current user"). Point HOME at the root-owned /root, as Playwright
      # advises. Scoped to this step only — a job-level HOME leaks to the host
      # runner and breaks its docker config lookup (/root/.docker/config.json).
      - name: Accessibility e2e (axe)
        working-directory: client
        env:
          HOME: /root
        run: npm run test:e2e

      - name: Upload accessibility reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: frontend-a11y-reports
          path: |
            client/playwright-report
            client/.lighthouseci
          if-no-files-found: ignore

  # Production deploy gate: this job only runs after both build jobs above
  # succeed, and only on a push to main or a manual run. A red CI therefore
  # blocks the deploy. Manual runs (workflow_dispatch) still run CI first.
  #
  # Deploy strategy (post-ACA): the client and the .NET 8 managed functions both ship to a SWA Free
  # tier preview environment ("staging"); the Playwright smoke drives the create → share → restore
  # flow against that live preview; on success the same built artifacts are promoted to production
  # (no rebuild); on failure production keeps the old version. There is no container app to blue-green.
  deploy:
    name: Deploy to Azure
    needs: [build-and-test, build-frontend]
    # Only ever deploy main: a push to main, or a manual run targeting main.
    # workflow_dispatch can be launched from any branch, so guard the ref too.
    if: >-
      github.ref == 'refs/heads/main'
      && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    # Hard cap so a hung step (e.g. a wedged smoke test) can never run for hours and
    # hold the deploy-production concurrency lock. Generous enough for provision + the
    # preview/promote SWA deploys.
    timeout-minutes: 30
    # Serialize production deploys so overlapping merges to main don't run
    # concurrent azd provision/deploy against the same environment.
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    permissions:
      id-token: write
      contents: read
      # read: pull the prebuilt frontend-ci image (Playwright browsers baked in) for the client
      # smoke step below. No image is pushed anymore — the ACA GHCR API image is gone.
      packages: read
    env:
      AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      AZURE_ENV_NAME: ${{ vars.AZURE_ENV_NAME }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
      DOTNET_NOLOGO: true
      DOTNET_CLI_TELEMETRY_OPTOUT: true
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Install azd
        uses: Azure/setup-azd@634ad924cf8baef2257898ba5663be8d19f15aca # v2

      - name: Setup .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          dotnet-version: '10.x'

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          # Match the Node version CI validates the client under (build-frontend
          # uses 24) so the shipped artifact is built on the same runtime.
          node-version: '24'

      - name: Log in with Azure (Federated Credentials)
        run: |
          azd auth login \
            --client-id "$AZURE_CLIENT_ID" \
            --federated-credential-provider "github" \
            --tenant-id "$AZURE_TENANT_ID"

      # ── Infra ────────────────────────────────────────────────────────────────
      # Provision the trimmed subscription-scoped infra (Cosmos + Log Analytics/App Insights + the
      # Static Web App, which now hosts the managed functions and their app settings). No container
      # app, user-assigned identity, or ACA environment remains. The Cosmos key and App Insights
      # connection string are wired into the SWA app settings inside the bicep — never surfaced as
      # CI-visible outputs. The az CLI login and az/containerapp deployments are gone with ACA.
      - name: Provision Infrastructure
        run: azd provision --no-prompt

      # ── Build artifacts ──────────────────────────────────────────────────────
      # Client: same-origin /api base — SWA serves the app and its managed functions from one origin,
      # so axios get('/share-factory') resolves to /api/share-factory. No ACA-domain lookup. Built
      # ONCE here; uploaded twice (preview → production) without a second build.
      - name: Build web client
        run: |
          set -euo pipefail
          echo "VITE_API_BASE_URL=/api" > client/.env.production
          npm ci --prefix client
          npm run build --prefix client

      # Functions: pre-publish the .NET 8 isolated worker so the SWA action deploys the built output
      # (skip_api_build) rather than asking Oryx to build it. staticwebapp.config.json pins
      # platform.apiRuntime to dotnet-isolated:8.0 — dotnet-isolated:9.0 fails to deploy as a SWA
      # managed function ("Failed to deploy the Azure Functions"; verified via a minimal repro).
      - name: Publish managed functions
        run: dotnet publish api.functions/api.functions.csproj --configuration Release --output ./api-publish

      # Upload the built client + managed functions to a named SWA preview environment ("staging").
      # The action's static_web_app_url output exposes the preview URL for smoke testing. Both
      # app_location and api_location point at pre-built output, so skip_app_build/skip_api_build
      # are set (the SDK on the runner built them; Oryx doesn't rebuild).
      - name: Deploy client + functions to SWA preview (staging)
        id: swa_preview
        uses: Azure/static-web-apps-deploy@1a947af9992250f3bc2e68ad0754c0b0c11566c9 # v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: client/dist
          skip_app_build: true
          api_location: api-publish
          skip_api_build: true
          deployment_environment: staging

      # Readiness gate: cheap HTTP poll until the SWA preview serves the app shell
      # (HTTP 200 + the React root div). This waits out SWA preview propagation so
      # the browser smoke below launches against a live deployment instead of racing
      # the CDN. Bounded by retries + timeout-minutes.
      - name: Wait for client preview to be live
        id: client_live
        timeout-minutes: 5
        env:
          PREVIEW_URL: ${{ steps.swa_preview.outputs.static_web_app_url }}
        run: |
          set -euo pipefail
          echo "Waiting for client preview: $PREVIEW_URL"
          for attempt in $(seq 1 10); do
            code=$(curl -sS -o /tmp/preview-index.html -w '%{http_code}' --max-time 30 "$PREVIEW_URL/" || echo "000")
            if [ "$code" = "200" ] && grep -q 'id="root"' /tmp/preview-index.html; then
              echo "Preview is live: HTTP 200 and app shell present."
              exit 0
            fi
            echo "attempt $attempt: http=$code, app shell not ready yet; retrying in 10s..."
            sleep 10
          done
          echo "ERROR: client preview did not serve the app shell (last http=$code)." >&2
          exit 1

      # GHCR login so the smoke step can pull the prebuilt frontend-ci image.
      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      # Functional smoke against the REAL deployed preview + live managed functions: drives the
      # create -> share -> restore flow (client/tests/smoke.spec.ts) in a browser, asserting
      # POST /api/share-factory returns a key and the share link restores via
      # GET /api/shared-factories/{key}. This is the post-deploy validation the pre-deploy e2e suite
      # (local dev server, mocked) can't give us — and it exercises the functions end-to-end.
      #
      # It runs inside the SAME prebuilt frontend-ci image build-frontend uses
      # (Playwright browsers + system libs baked in) via `docker run`, so browsers
      # are NEVER installed on the bare deploy runner — that bare-runner install is
      # exactly what stalled the original in-job smoke. Guardrails: CI=true selects
      # Playwright's non-blocking `line` reporter (the `html` reporter's report
      # server hung the runner on failure); --ipc=host is Playwright's Chromium
      # memory flag; per-test/expect timeouts live in playwright.smoke.config.ts;
      # timeout-minutes caps the step; `docker run`'s exit code propagates pass/fail.
      # The image's baked deps (/opt/ci/node_modules) replace the host client deps
      # so the installed Playwright matches its baked browsers (the build-frontend
      # pattern). The host's client/node_modules already exists from the earlier
      # `npm ci --prefix client`, so it is removed first to avoid a nested copy.
      - name: Smoke-test client (Playwright, deployed preview)
        id: client_smoke
        timeout-minutes: 8
        env:
          PREVIEW_URL: ${{ steps.swa_preview.outputs.static_web_app_url }}
          CI_IMAGE: ghcr.io/greven145/yet-another-factory-planner/frontend-ci@sha256:56b9e36b41b193f3f911b85d8af6a67867b506b974b3ad66a3e1864ae7ee85f4 # :latest
        run: |
          set -euo pipefail
          echo "Smoke-testing client preview in browser: $PREVIEW_URL"
          docker run --rm --ipc=host \
            -e CI=true \
            -e SMOKE_URL="$PREVIEW_URL" \
            -v "$PWD/client:/client" \
            -w /client \
            "$CI_IMAGE" \
            sh -c 'rm -rf ./node_modules && cp -a /opt/ci/node_modules ./node_modules && npm run test:smoke'

      # Promote the already-built client/dist + functions to production by re-uploading
      # WITHOUT the deployment_environment input.  No rebuild occurs.
      # Explicitly gated on the client smoke succeeding so production keeps the
      # old version on a smoke failure.
      - name: Promote client + functions to SWA production
        if: steps.client_smoke.outcome == 'success'
        uses: Azure/static-web-apps-deploy@1a947af9992250f3bc2e68ad0754c0b0c11566c9 # v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: client/dist
          skip_app_build: true
          api_location: api-publish
          skip_api_build: true
          # No deployment_environment → targets the production slot.
